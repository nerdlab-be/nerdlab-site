<div class="cd-schedule cd-schedule--short loading">
  <div class="timeline">
    <ul>
      <li><span>12:00</span></li>
      <li><span>12:30</span></li>
      <li><span>13:00</span></li>
      <li><span>13:30</span></li>
      <li><span>14:00</span></li>
      <li><span>14:30</span></li>
      <li><span>15:00</span></li>
      <li><span>15:30</span></li>
      <li><span>16:00</span></li>
      <li><span>16:30</span></li>
      <li><span>17:00</span></li>
      <li><span>17:30</span></li>
      <li><span>18:00</span></li>
      <li><span>...</span></li>
      <li><span>20:00</span></li>
    </ul>
  </div>

  <div class="events">
    <ul>
      {% set talks = craft.entries.section('mutationProgramDetails').all() %}

      <!-- MediaFoyer -->
      <li class="events-group">
        <div class="top-info">
          <span class="c-top-info__title">{{ mutationGlobals.timetableLabels.one().kraaienest }}</span>
          {% if mutationGlobals.timetableLabels.one().kraaienestInfo | length() %}
          <span class="c-top-info__info">{{ mutationGlobals.timetableLabels.one().kraaienestInfo }}</span>
          {% endif %}
        </div>
        <ul class="js-sort">
          {% for item in talks %}
          {% if item.mutationRoom == "kraaienest" %}
          {% include '_components/components.timetable-entry.twig' %}
          {% endif %}
          {% endfor %}
        </ul>
      </li>

      <!-- Logos -->
      <li class="events-group">
        <div class="top-info">
          <span class="c-top-info__title">{{ mutationGlobals.timetableLabels.one().logos }}</span>
          {% if mutationGlobals.timetableLabels.one().logosInfo | length() %}
          <span class="c-top-info__info">{{ mutationGlobals.timetableLabels.one().logosInfo }}</span>
          {% endif %}
        </div>
        <ul class="js-sort">
          {% for item in talks %}
          {% if item.mutationRoom == "logos" %}
          {% include '_components/components.timetable-entry.twig' %}
          {% endif %}
          {% endfor %}
        </ul>
      </li>

      <!-- Industriemuseum -->
      <li class="events-group">
        <div class="top-info">
          <span class="c-top-info__title">{{ mutationGlobals.timetableLabels.one().industriemuseum }}</span>
          {% if mutationGlobals.timetableLabels.one().industriemuseumInfo | length() %}
          <span class="c-top-info__info">{{ mutationGlobals.timetableLabels.one().industriemuseumInfo }}</span>
          {% endif %}
        </div>
        <ul class="js-sort">
          {% for item in talks %}
          {% if item.mutationRoom == "industriemuseum" %}
          {% include '_components/components.timetable-entry.twig' %}
          {% endif %}
          {% endfor %}
        </ul>
      </li>

      <!-- Tejo -->
      <li class="events-group">
        <div class="top-info">
          <span class="c-top-info__title">{{ mutationGlobals.timetableLabels.one().tejo }}</span>
          {% if mutationGlobals.timetableLabels.one().tejoInfo | length() %}
          <span class="c-top-info__info">{{ mutationGlobals.timetableLabels.one().tejoInfo }}</span>
          {% endif %}
        </div>
        <ul class="js-sort">
          {% for item in talks %}
          {% if item.mutationRoom == "tejo" %}
          {% include '_components/components.timetable-entry.twig' %}
          {% endif %}
          {% endfor %}
        </ul>
      </li>

    </ul>
  </div>
</div>
<div class="schedule-evening-holder">
  <ul class="o-list-bare">
    {% for item in talks %}
    {% if item.mutationRoom == "avondprogramma" %}
    <li class="schedule-evening-block">
      {% if item.articleLink | length() %}
      {% for page in item.articleLink.all() %}
        <a href="{{ page.url }}">
          <span class="event-date">{{ item.hourStart|date('H:i') }} - {{ item.hourEnd|date('H:i') }}</span>
          <span class="event-name">
            {{ item.title }}
          </span>
        </a>
      {% endfor %}
      {% else %}
        <span class="event-date">{{ item.hourStart|date('H:i') }} - {{ item.hourEnd|date('H:i') }}</span>
        <span class="event-name">
          {{ item.title }}
        </span>
      {% endif %}
    </li>
    {% endif %}
    {% endfor %}
  </ul>
</div>
